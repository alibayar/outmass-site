fastapi==0.115.6
uvicorn==0.34.0
supabase==2.11.0
python-jose[cryptography]==3.3.0
python-multipart==0.0.20
celery==5.4.0
redis==5.2.1
stripe==11.4.1
httpx==0.28.1
jinja2==3.1.5
python-dotenv==1.0.1
posthog==3.7.0
anthropic==0.42.0
# CSV encoding detection for /campaigns/detect-encoding. Was already present
# transitively via requests; pinned explicitly because a behaviour change in
# it changes what a user's contact list decodes to.
charset-normalizer==3.4.4
