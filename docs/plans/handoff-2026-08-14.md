# Session Handoff — 2026-08-14

Continuity doc; previous: `handoff-2026-08-13.md`. Companions: `backlog.md`,
`railway-env.md`. Internal — `docs/plans/` never ships publicly.

A long day. Seven backend commits, all unpushed, plus the Stripe investigation
that answered a question we had been circling for a month. Stopped before
starting the email-template refactor because it touches ten live transactional
emails across four files and a half-done version of that is the worst place to
pause.

---

## State right now

| | |
|---|---|
| `origin/master` | **38400c5** — deployed |
| Local `master` | **13 commits ahead**, **NOT PUSHED** (`git log origin/master..HEAD`) |
| Working tree | clean |
| Tests | **1,061** backend, 11 extension suites, 81 e2e — all green |
| Extension zip | **0.2.1 packaged, verified, NOT uploaded** |
| Extension master | **0.2.2** — bumped so it stops colliding with the zip |
| Chrome / Edge | 0.2.0 / 0.1.26 |
| Migrations | **028, 029, 030 written AND run by Ali today** |

**Twelve of the thirteen unpushed commits are backend.** Railway auto-deploys
on push, so pushing them is a production deploy. They are individually tested
and independent; nothing is half-finished.

```
50668d5 chore(rel)    master is 0.2.2, so it stops calling itself 0.2.1
577262a feat(i18n)    the ten emails, in thirteen more languages
7e7c8d8 feat(i18n)    learn what language to write to someone in
b5d937e refactor      one body per template, both parts derived from it
307ae6d docs(handoff) this file
75b752a test(email)   pin all ten transactional templates before moving them
79398b6 fix(quota)    anchor stops decaying; leaving customer loses the bonus month
f0790d1 feat(db)      three columns the remaining work is blocked on
03208ad fix(auth)     two ways a Microsoft connection died with nobody told
65b35f8 fix(billing)  one customer, one subscription — both routes closed
b85ff2a fix(billing)  the success page tells the customer what happened
09a4b08 fix(ops)      a startup alert says which service it came from
```

**The 0.2.1 zip is untouched and is still the one to upload.** `50668d5` moved
master to 0.2.2 only so the two builds cannot both claim 0.2.1 — the zip has
no `X-UI-Language`, master does. Nothing inside the zip changed.

`inbox/` is where Ali drops files for a single question, now that Downloads is
denied by a rule in `.claude/settings.local.json` (verified to bite; Bash there
is blocked too).

---

## The Stripe investigation — closed

The month-old question "why do we lose people at checkout" has an answer, from
all six `checkout.session.expired` payloads read in Workbench.

**The discriminator is not `payment_intent`** — it is null on every one of
them, because in subscription mode the charge rides the Invoice, not a session
PaymentIntent. It is the **customer / subscription / invoice triple**, which
Stripe only creates when the payment form is submitted.

| created (UTC) | who | card entered | Stripe branding shown |
|---|---|---|---|
| 07-16 22:23 | ekaynimos@hotmail.com (NL) | no | Metis Information Technologies Ltd |
| 07-29 13:44 | Kevin.McCann@mercedesscientific.com | **YES → declined** | Metis |
| 07-30 15:28 | marketing@mercedesscientific.com | no | Metis |
| 08-03 15:09 | Kevin.McCann@mercedesscientific.com | **YES → declined** | Metis |
| 08-10 07:14 | anbu.atomix@outlook.com (India) | no | Metis |
| 08-12 05:55 | flaviorfdc0406@outlook.com (Brazil) | no | **OutMass** |

**Four of five people opened the payment page and typed nothing.**
`customer_details.name/address/phone` are null on all four; the only populated
field is the email we pre-filled ourselves.

**Kevin is the only payments case.** Two attempts, two failures. 07-29:
MasterCard ••5683, Citibank N.A., decline code `transaction_not_allowed`; he
tried Link first and **Stripe's own risk engine blocked it**. 08-03: declined
by the issuing bank again. `transaction_not_allowed` means the card does not
permit this type of transaction — a corporate card with merchant controls, or
one closed to cross-border or recurring charges. Nothing on our side to fix.

**The descriptor hypothesis is dead, checked and wrong.** Stripe → Business
details shows trading name `OutMass` and statement descriptor `OUTMASS`
already. The bank saw a fine name.

**Five of six sessions showed "Metis Information Technologies Ltd"** on the
checkout page — the trading name was changed 08-11 and the payloads either
side bracket it exactly. Not the whole story: flaviorfdc0406 saw the corrected
name and still typed nothing.

### What it means

Every addressable loss is upstream of Stripe. Payments, pricing and Stripe
configuration have each been eliminated with evidence. **0.2.1 is aimed
exactly at the moment four of five people left**, and uploading it is the
highest-leverage single action available.

Also corrected: **tony@skylineprp.com is a paying customer**, not a lost lead.
He had an abandoned session from 07-20 and was wrongly counted in the earlier
"five people" list. Real list is five, and two of them are the same company
(mercedesscientific.com).

This diagnosis was only possible because `checkout_session_created` was added
on 07-20. Without it these five read as "clicked upgrade, then nothing".

---

## What shipped today (unpushed)

**Startup alerts name their service** (`09a4b08`). The guard added yesterday
fired within hours: a TEST Stripe key against the production database — the
exact condition it was written for, and its own docstring records the same
thing happening on the WORKER on 2026-08-10. It did not say WHICH service.
Railway variables are per-service; an alert that leaves you guessing is half
an alert. **Ali still has to find and fix that key** — see open actions.

**The success page tells the truth** (`b85ff2a`). `/billing/success` ignored
its own `session_id` and told everyone "your plan upgrade is now active". True
for a card in a second; false for a bank debit for days, while the account
sits on Free. Someone reading that, then seeing Free, pays again. It now reads
the session and says one of three true things, all non-settled paths carrying
"You don't need to pay again." Gated on `_SETTLED_PAYMENT_STATUSES`, the same
constant the webhook grants on, so a status Stripe invents later lands in the
cautious branch.

**One customer, one subscription** (`65b35f8`). Two routes, both closed.
(a) The webhook replay guard is single-slot — it asks only "is this the id
already on the row", so a *different* subscription reads as a first-time
activation and would overwrite the live one and re-zero quota mid-period. Now
it checks whether the stored one is still live in Stripe and, if so, writes
nothing and alerts with both ids. `_subscription_still_live` answers True when
it cannot tell, deliberately. (b) `create-checkout` swallowed a StripeError
into `sub = None`, which the status check read as "not subscribed" and opened
a second full-price session for an existing subscriber. Now 502s.

**Two ways a Microsoft connection died silently** (`03208ad`, closes #42).
(a) Microsoft ROTATES refresh tokens; the persist sat inside a try catching
only `httpx.HTTPError`, so a DB failure escaped into the caller's unguarded
per-contact loop with the new token unsaved and the old one spent. Now its own
try, returns the access token anyway, and alerts **only when the token was
actually rotated** — an unchanged one failing to persist costs nothing.
(b) A first sign-in returning no refresh token wrote no row and only logged.
The user has a JWT and a panel saying "connected" while every later token use
finds nothing. Now flags `requires_reauth` with reason
`no_refresh_token_on_first_signin` — the diagnostic whose absence is why #42
sat unanswerable.

**Three migrations** (`f0790d1`) — written, and **Ali ran all three**.
`cancel_at_period_end`, `month_reset_anchor_day`, `preferred_language`. The
029 verification query came back clean: no decayed anchors, so the backfill was
correct.

**Two quota defects** (`79398b6`). The anchor no longer decays — a 31st anchor
survives February instead of becoming a permanent 28th, and `next_reset_date`
uses the real day because that date is quoted to users in the quota-cap email.
And a subscription Stripe ends today no longer rolls over first: held for
**exactly one day**, so a lost `subscription.deleted` cannot strand someone at
zero quota forever.

**Ten email templates pinned** (`75b752a`). Every word of every transactional
email, subject/text/html, compared verbatim. This is the safety net under #36.

---

## #36 — DONE, all three steps

1. **One source per template** (`b5d937e`). `backend/emails/`: `render.py`
   turns a block list into text and HTML, `catalog.py` says which blocks each
   of the ten has, `strings/<lang>.json` holds every word. Markup is
   deliberately three things — `**bold**`, `` `code` ``, `[label](url)`.
   Variables are `${name}` (string.Template) because the welcome email
   contains the literal merge tag `{{firstName}}`, which under `.format` would
   need `{{{{firstName}}}}` in fourteen files.
2. **The language travels** (`7e7c8d8`). One header, `X-UI-Language`, next to
   the `X-Extension-Version` header that already rode there. Stored via
   `maybe_touch_activity` on the changed-value path, not the 15-minute timer.
3. **Thirteen translations** (`577262a`). ar, de, es, fr, hi, ja, pt_BR,
   pt_PT, ru, tr, zh, zh_TW — 89 keys each, covering all fourteen panel
   locales (zh_CN → zh; zh_HK/zh_MO aliased to zh_TW so they do not fall back
   onto the wrong script).

**The five HTML-only templates now have a text part**, which was the finding
from the pinning commit. `HTML_ONLY` is empty and a test asserts it stays
empty, so a future single-part template has to be a written decision.

### What it changed for a reader, and what it did not

The goldens pin bytes, and bytes had to move: text is wrapped by `textwrap`
now, and there is one HTML shell where there were three. So the check that
mattered was a **word-level diff of old golden against new render**, tags
stripped. Seven of ten templates came out word-identical in every part. The
three differences were all the same thing — the text and HTML halves of a
template had drifted and one had to win:

* `welcome`: HTML said "Thanks for signing in!", text said "…to OutMass!";
  HTML lacked "In the panel," in step 2 and "you can" in the free-plan line.
  The text version won.
* `upgrade`: the third bullet promised slightly different things in each.
  The HTML version won.
* `welcome` P.S.: "ON THE WEB" in caps became bold, which the text part drops.

Nobody had noticed any of it, because nobody reads both.

### Two things still open under it

* **The welcome email cannot be localised yet.** A row created seconds ago has
  never sent a header. Closing it means carrying the locale through the OAuth
  `state` parameter — an auth-flow change, deliberately not folded in here.
  `test_new_user_gets_welcome_email` pins the `None` so it stays visible.
* **The translations are one person's work and are not equally strong.**
  Turkish is the one to read first — it is the only one that can be judged
  here, and it is the canary for register. Nothing reaches a customer until a
  build sending `X-UI-Language` propagates through the store, so there is time.

---

## Ali's open actions

1. **Find the TEST Stripe key.** Check all three Railway services'
   `STRIPE_SECRET_KEY` for an `sk_test_` prefix. Web is the one that matters —
   it creates checkouts. Worker/beat carry no runtime risk (verified: workers
   never call the Stripe API, only read `stripe_subscription_id` as a column)
   but should be fixed anyway. The re-pushed alert will name the service.
2. **Push the 13 commits** — this is a production deploy.
3. **Upload `outmass-0.2.1.zip` to Chrome.** Now evidence-backed as the
   highest-leverage action available. Upload the zip exactly as it is; master
   is 0.2.2 and the language capture ships there, not in this one.
4. **Write to the five prospects.** Two messages: Kevin's payment failed and
   we know why; the other four never got that far. All BCC
   `outmassapp@outlook.com`.
5. **Publish 0.2.0 on Edge** — gates #24 and #48.
6. **Shared Variables** in Railway (see `railway-env.md`) — the fix that makes
   incident 1 unable to happen a fourth time.
7. **#28** store-listing paste.

---

## Carried forward

- **#41** Lucia hits the 30-day nudge around 08-26.
- **#48 is largely SHIPPED and the backlog entry is stale** — `background.js`
  forwards `mcode`, `i18n.js` maps 14 classifications to sentences, and
  `ms-auth-codes` pins it against the backend taxonomy. The only gap is
  `bad_request_method`, which is known, commented, has a graceful fallback, is
  attributed to "app" (our bug if it fires), and has never fired. Not worth a
  key in 14 files.
- **Stripe customer reuse** — skipped on Ali's call. `create-checkout` passes
  `customer_email` and never `customer=`, so every retry mints a new Stripe
  customer (Kevin has two). No live risk: `stripe_customer_id` is written only
  on a completed checkout, so our DB points at neither orphan and webhooks
  about them match no user. Costs Stripe's own retry logic its signal.
- ~~Two backlog entries are stale~~ — **cleaned 2026-08-14.** DMARC is marked
  done (it was still headed "CONFIRMED MISSING" the day after it was fixed),
  and api.getoutmass.com is closed with #27. Both now carry a line saying they
  were stale, because a backlog that quietly corrects itself teaches nothing.
- **bellmed** reset today as designed — `month_reset_date` was 2026-07-14, the
  rollover fires from the scheduled beat without them logging in. Worth one
  look that `emails_sent_this_month` restarted from 0 and is climbing; if it
  still reads 2243, the beat is not picking up their campaign and the question
  is why it is not `scheduled`, not the quota.
- **Funnel data before 2026-08-08** remains unusable for any tight event pair.

---

## What today taught, beyond the fixes

The recurring shape held all day and turned up inside my own work twice.

- The mapping review found a guard I had just written was **already violated in
  latent form**: `send_email_task` could send mail charging no quota, was
  registered with Celery, and was one `send_task()` from live. Deleted.
- The sender-detection test I wrote to replace it **matched nothing at all**
  and passed clean — the real senders go through `async_post_with_retry`, not
  `.post()`. A check that silently matches nothing is the worst of the three
  outcomes, and only its own self-test caught it.
- The golden test's update flow was a `--flag` declared in a test file, which
  pytest silently ignores. It documented a workflow that did not exist.

Each was found by running the thing rather than reading it. The habit that
keeps paying: **check the thing whose failure would be silent, especially when
you wrote it.**
