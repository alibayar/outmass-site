# Session Handoff — 2026-07-26 (pre-compaction)

Continuity doc; previous: `handoff-2026-07-15.md`. Companions: `backlog.md`,
`2026-07-19-marketing-roadmap-draft.md` (+ HeyReach addendum),
`store-featuring-playbook.md`. Internal — `docs/plans/` never ships publicly.

---

## Git / deploy state

- **origin/master = d0dd2b9** — everything through it DEPLOYED (Railway web +
  worker). **UNPUSHED local: 39218b9 + 7eb954e** (HeyReach roadmap docs) and
  this handoff commit — ride the next "push" (docs-only, no deploy effect).
- **Chrome: 0.1.26 LIVE** (silent update, published 07-18; rollout healthy —
  SA/GB/US/NG/IN devices observed updating; re-approve prompts being clicked).
- **Edge: 0.1.26 IN REVIEW since 07-19.** 7-business-day window ends EOD
  **Tue 07-28**. One-time reminder task `edge-review-sla-check` fires
  **Wed 07-29 10:00 TR** with a ready-to-paste GitHub issue (documented
  channel: MicrosoftEdge-Extensions repo Issues). Do NOT withdraw/resubmit
  (queue reset — burned once already). Edge cohort (dominic PH, ekaynimos NL,
  BR/PH anons) still on 0.1.24 → no chips/sign-in-gate/OneDrive guard for them.
- No pending migrations. Stripe live webhook now also receives
  `checkout.session.expired` (Ali added). Sandbox webhook deleted (lived in a
  SECOND sandbox — "Import · 1" in Workbench was the clue).

## Shipped since last handoff (all live)

**Extension 0.1.26 (07-18):** merge-tag chips (clickable {{column}} under
editor, insert-at-cursor + updateSendButton fix), sliding-session pickup
(X-Refresh-JWT), OneDrive picker one-shot OAuth guard + oneDriveAuthStuck ×11,
honest popupConsentExplainer ×11, zh Simplified-script fix (adversarial review
catch). Release used the multi-lens adversarial review — now a GLOBAL rule in
user CLAUDE.md + memory `release_adversarial_review`.

**Backend (rolling deploys):**
- JWT sliding refresh: valid token past half its 24h life → fresh one in
  X-Refresh-JWT (get_current_user); extension stores it (guarded against
  post-logout resurrection). Active users never hit the daily sign-in wall.
- Reply-cancels-followup: followup_worker excludes replied_at for EVERY
  condition. Site now truthfully claims it (pricing Pro bullet + index card).
- Report: paywall split — feature_locked* error codes render "(info · paywall)",
  never raise ⚠️ (HogQL GROUP BY event, paywall).
- Checkout funnel telemetry: checkout_session_created (create-checkout) +
  checkout_abandoned (checkout.session.expired webhook; already_subscribed
  guard) + INFO list in report. Field-proven (ekaynimos, tony false-positive).
- CSV header-strip on upload (padded " email"/" Company" headers work now).
- OneDrive: callback persists access_token + has_onedrive_scope even when
  Microsoft omits refresh_token (consent-loop root cause); license-403 →
  no_onedrive; auth-blocked bodies logged.
- **Auto-resume beat** (06:00 UTC daily): recent 'partial' campaigns flip to
  'scheduled' when owner regains headroom (check_monthly_reset runs FIRST —
  reset lands even if user never logs in). 14-day age guard.
- **Quota-cap email**: capped /send → "N recipients saved, they'll send
  automatically on <reset date>" (welcome_email.send_quota_capped_email).
- **Strand fix (c1705f2)**: quota-capped CLEAN sends now land 'partial' (they
  closed 'sent', hiding pending from BOTH resume paths — the Faisal bug).
- **Payment-failed Telegram alert (d0dd2b9)**: renewals that decline now ping
  operator instantly (was a silent log line).
- Test hygiene: conftest blanks POSTHOG_API_KEY + posthog.disabled — local
  pytest can no longer pollute prod telemetry (3 fake $exception incident).

## THE FAISAL SAGA (active, watch daily)

Timeline: 07-20 capped at exactly 2500 (batch sent, 250 pending) → campaign
closed 'sent' (strand bug) → 07-25 ~20:16 renewal SOFT-DECLINED ("Try again
later", Wise-issued card?) → webhook customer.subscription.updated(past_due)
→ **plan=free now** → 07-26 ~01:00 diagnosis: SQL rows showed sent_count 750,
counter 2500, anchor 2026-06-25 → strand fix + alert shipped; Ali flipped
campaign to 'partial' via SQL → **07-26 morning beat WORKED**: anchor→
2026-07-25 ✓, counter 0→130, sent_count 750→880, status 'partial' (daily
drainage). State: ~330 contacts not-yet-sent (mix of real + suppressed-skip
strays), free headroom ~120 → tomorrow's beat sends more; FULL drain when
Starter restores (Stripe Smart Retries in progress; success auto-restores via
subscription.updated=active; another failure now alerts Telegram).
- Ali decided: no "our bug" confession in the email (customer never received
  the auto-resume promise; his experience was better-than-promised). Final
  email text + targeted in-app announcement SQL delivered 07-26 — **sending
  status unconfirmed** (check with Ali).
- Interpretation guide for daily SELECTs: good = sent_count climbing,
  status partial-or-sent-with-empty-resumables, counter tracking sends.

## Known open defects (documented, prioritized)

1. **scheduled_worker final-status has the same early-close weakness** as the
   fixed router path ('sent' if no errors — quota-slice leftovers can strand).
   Didn't fire on 07-26 (errors kept it partial) but MUST be fixed calmly.
2. **Suppressed/unsubscribed skips stay 'pending' forever** (both send loops
   `continue` without marking) — inflates resumable sets; harmless one-cycle
   churn with auto-resume; fix = mark at skip time (Reports semantics TBD).
   Both in backlog with full notes.

## Users / revenue snapshot

- **Paying (5 × $9 = $45):** Greg · Mary · bellmed · Lucia · **Tony**
  (tony@skylineprp.com, 07-20 — **skylineprp = first 2-seat company**; Tony
  heavy chip user, 2 campaigns 07-23, JWT-expiry re-login worked as designed).
- **Faisal:** free (payment saga above) — restores to Starter on retry success
  → $54. THE upsell candidate (3k+/cycle vs 2500 Starter) once stable.
- Gifts: Surendra expired 07-22 (beat, unconfirmed visually) · Miriam expired
  07-24 ✓ (Ali confirmed; she went silent 06-29 — NO farewell email by
  decision) · Alan pro until ~08-06.
- New/notable: dominic (PH, Edge 0.1.24) — 1h merge-tag struggle,
  self-recovered, used OneDrive cleanly (backend fix proof); randallrealty
  (install→login <1 min); lucyzhao updated 0.1.26; aeezo/Charlotte silent.
- **Consent-funnel leak = top-of-funnel issue #1**: ~5 anonymous losses AT the
  Microsoft consent screen in 10 days (AU/BR/TR×3/PH; PH one had near-perfect
  activation: 29-recipient CSV + 6 chips in 5 min, then bailed). Backlog item
  with UX ideas written 07-26 (Ali: "yaz").

## Ops / monitoring

- Telegram report (05/17 UTC) + **outmass-error-triage** scheduled task
  (08:35/20:35 TR daily; reads HARD/INFO lists from daily_report.py; knows
  benign patterns: paywall codes, consent_declined, merge-tag recoveries,
  pre-0.1.25 not_authenticated).
- Scheduled one-times pending: `edge-review-sla-check` (07-29 10:00).
- Note: scheduled tasks run only while the app is open (Faisal verify ran 11h
  late for this reason).
- PostHog CANNOT see server-side worker sends — Supabase SELECTs are the
  ground truth for resume/drainage checks (this bit us: "no news" ≠ "sent").
- Stripe payouts: GBP → WISE ****5966 (60-84-64). Ali investigating payout
  schedule (£0 at Wise — likely pending-settlement or manual schedule;
  walked through Balance/Payouts checks 07-26).

## Marketing state

- **Roadmap draft** (57-idea multi-lens brainstorm, judged, Turkish doc) at
  `2026-07-19-marketing-roadmap-draft.md` — **still awaiting reconciliation**
  with pre-existing notes (Ali will say "uzlaştır").
- Consolidated stack layers presented 07-26 (chat): Foundation → Capture →
  Content → PLG → Paid (Bing-first: audience-medium match, cheap CPCs,
  competitor gap, compounding with Bing SEO/Copilot) → Partnerships → Parked.
- **APPROVED next assets (execution order):** (1) "LinkedIn + Outlook outreach
  stack (HeyReach + OutMass)" article — content-engine slot #1; zero-code true
  workflow (HeyReach CSV export → OutMass sends); claims-discipline strict;
  (2) HeyReach outreach email — AFTER article is live. Ali triggers with "yaz".
- HeyReach analysis in roadmap addendum: NOT a competitor (LinkedIn channel,
  same buyer); no LinkedIn-automation entry for us (ToS/ban risk); lessons:
  flat-fee agency pricing (future Team plan idea), ecosystem gap = no
  Outlook-native email leg.
- Store-listing refresh: 9-point audit in backlog, **trigger = Edge 0.1.26
  publish** (stale free=50→250, Starter 2000→2500, killed timezone claim
  alive in translations, missing Pro-only label + daily-cap bullet, language
  count, verified 30-day refund = real). pt-BR listing translation rides it.
- Locales approved for 0.1.27: **pt_BR + pt_PT** (data: only uncovered locale;
  NL deliberately skipped — both NL users self-selected English). Featuring
  playbook parked with triggers (listing polish → ~10 reviews → nominate).

## Process rules (stable + additions)

- Approval before user-affecting changes; NO push without Ali's "push"; money/
  credentials/store uploads/SQL = Ali. Support: async only; BCC
  outmassapp@outlook.com; gift-test; claims discipline (see memories).
- **NEW global rule (user CLAUDE.md): multi-lens adversarial review before
  every release/deploy cut, all projects** — lenses independent, ≥2 skeptics
  per finding (default refuted), cheap hardenings from refuted pile, sweep
  agent litter before packaging (zhout.txt lesson).
- Backend tests: run SPECIFIC files with JWT_SECRET env (full-suite collection
  breaks); conftest now telemetry-isolated. Commit trailer:
  `Co-Authored-By: Claude Fable 5 <noreply@anthropic.com>`. PowerShell
  here-strings with quotes/`-` lines break → use `git commit -F <file>`.

## Wake-up checklist for next session

1. Faisal drainage: daily SELECT pair (campaign 49587d65… + user row);
   Stripe retry outcome (alert or dashboard); email/announcement sent?
2. Edge review: approved? → store-listing refresh pass + Edge-cohort watch
   (ext_updated to_version=0.1.26 browser=Edge). Not approved by 07-29 →
   reminder task hands Ali the GitHub issue.
3. Morning triage reports (2×daily) — paywall codes now info; checkout_
   abandoned entries have already_subscribed context.
4. Calendar: Alan promo ~08-06 · next quota anniversaries roll silently
   (auto-resume handles capped leftovers).
5. On Ali's word: "yaz" → stack article · "uzlaştır" → roadmap reconciliation
   · 0.1.27 cut (quota-text ×13 + pt locales + review + zip) when he calls it.
