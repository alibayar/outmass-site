# Session Handoff — 2026-07-03 (pre-compaction)

Continuity doc so work resumes cleanly after compaction. Companions:
`backlog.md` (prioritized TODO) · `quora-content.md` (content drip) ·
`handoff-2026-06-24.md` (previous handoff, history) · root `deneyim.md`
(experience-transfer doc for the "what else could we build" ideation session).
Internal — `docs/plans/` is excluded from the public Jekyll site.

---

## Where things stand

### Git / deploy state
- **origin/master = `e7bd74c`** — DEPLOYED to Railway (health 200 verified).
  Everything is pushed; no branches in flight. Untracked local junk: release
  zips, `tenantId.pdf`, `.firecrawl/` (gitignored).
- **Backend deployed & live** (this stretch, newest first):
  `e7bd74c` billing-anchored rolling quota + `emails_sent_total` ·
  `8204028` quota-capped sends reported explicitly + English limit message ·
  `505d5e9` manual `/api/admin/trigger-report` ·
  `7f24c8f` twice-daily Telegram report (05:00+17:00 UTC = 08/20 TRT) ·
  `e313eff` install_source (chrome/edge/other) tagged at login ·
  plus the 06-25 batch (dedup fix, non-UUID tracking ids, benign-noise filter).
- **Extension:** Chrome store = **0.1.21 live**. **0.1.22 zip built**
  (`outmass-0.1.22.zip`, 27 entries/11 locales, verified) — Ali was uploading
  it to Chrome the night of 07-02/03; **confirm it's live**. **Edge = 0.1.20**
  (0.1.22 upload to Edge still pending — closes debounce/consent-explainer/zh
  gaps there).

### 🔴 IN-FLIGHT: the billing-anchored quota epic (finish first!)

**What changed (all deployed):** quota period is now a ROLLING month anchored
at `users.month_reset_date` (was: calendar month, login-only). Payment
(`checkout.session.completed`) re-anchors: all period counters zero + anchor =
pay day, replay-guarded (Stripe at-least-once). Reset check runs at /send gate,
scheduled_worker gate, GET /settings, GET /billing/status (sidebar polls
/settings — critical, its client-side guard blocks before /send). NEW
`users.emails_sent_total` = lifetime counter (never reset); RPC
`increment_user_sent_count` bumps both counters atomically; Python fallback is
deploy-order-safe. `/send` returns `quota_capped`/`quota_skipped`; extension
0.1.22 shows the explicit capped alert (11 locales). Was adversarially
reviewed (3-lens workflow): 1 blocker + 6 major fixed pre-commit; 4 bounded
findings deferred → backlog ("Quota follow-ups").

**Migration runbook state:**
1. ✅ `021_quota_billing_period.sql` RUN by Ali (verified: backfill produced
   `emails_sent_total`; Faisal 3030 = surviving contact rows, MORE accurate
   than the old counter-sum 2520 — old counter lost ~510 increments on crashed
   batches).
2. ✅ Code deployed (`e7bd74c`).
3. **⬜ VERIFY Ali completed the post-deploy SQL** (he was mid-execution at
   compaction):
   - `022_paid_anchor_alignment.sql` (bulk; Step-0 verified: Faisal's
     plan_updated_at day matches Stripe; Miriam is refunded → no active sub)
   - Faisal adjustment: `UPDATE users SET emails_sent_this_month = 1750 WHERE
     email='faisal@samaed.com';` (policy decision: he really delivered ~3,030
     in his Jun25→Jul25 cycle vs 2,500 Starter cap because OUR meter
     under-counted + wrongly reset Jul 1; Ali chose to leave him 750 = one
     more typical campaign, plus an honest email — drafted in chat, Ali sends)
   - Miriam combined fix (run AFTER 022; do NOT touch plan_updated_at — it's
     truthful history, Jun 26 = the cancellation webhook's touch):
     ```sql
     UPDATE users
     SET month_reset_date = '2026-06-24',
         emails_sent_this_month = 0,
         ai_generations_this_month = 0,
         manual_promo_until = '2026-07-24T00:00:00Z',
         stripe_subscription_id = NULL
     WHERE email = 'miriam@osbornecapitalpartners.com';
     ```
     Why: she paid Jun 24, was REFUNDED (~Jun 25) but her row is still `pro`
     (subscription.deleted never landed / cancel-at-period-end). We promised a
     free month of Pro. This aligns gift month = quota month = Jun24→Jul24,
     and the daily `expire_manual_promos` beat auto-downgrades her Jul 24 —
     BUT ONLY if `stripe_subscription_id` is NULL (the beat's "never touch a
     real paying customer" guard at scheduled_worker.py:671 skips rows with a
     sub id; hers is dead anyway).
   - Sanity SELECT afterwards: `SELECT email, plan, emails_sent_this_month,
     emails_sent_total, month_reset_date, manual_promo_until FROM users WHERE
     plan IN ('starter','pro') OR manual_promo_until IS NOT NULL;`

### 🔶 Telegram report — root-caused but NOT yet confirmed working
- Report existed all along but never arrived: **`TELEGRAM_BOT_TOKEN`/`CHAT_ID`
  were only on the Railway WEB service; Celery runs on WORKER** (services
  share code but NOT env vars — the uninstall ping worked because it's sent
  from web). Ali added them to worker. Worker log showed
  `Telegram not configured, skipping` before the fix.
- Schedule now `crontab(hour="5,17")` = 08:00/20:00 TRT; report enriched
  (active 7d/30d, +paid-today). **Verify a report actually arrives.**
- Manual test path deployed: `POST /api/admin/trigger-report` with
  `X-Report-Key: <REPORT_TRIGGER_KEY>` — fires via Celery `.delay()` so it
  exercises the WORKER env. **Requires Ali to set `REPORT_TRIGGER_KEY` on the
  WEB service** (404 until set; 403 on wrong key). Not yet tested.
- v2 (PostHog section in the report: installs, funnel, fails, locales) agreed
  to wait until 1-2 v1 reports arrive; needs a PostHog Personal API Key env.

### 📅 Dated follow-ups
- **Jul 24:** Miriam's gift Pro should auto-expire (promo beat) → verify she
  flips to `free` (and got the fresh-250 anchor Jul 24).
- **Jul 25:** Faisal's FIRST true rolling reset, same day as his Stripe
  renewal → verify counter zeroed + anchor advanced to Jul 25. This is the
  field proof of the whole quota epic.
- Faisal is an **upsell candidate**: real volume ~3k/cycle > Starter 2500; the
  now-accurate meter + capped-send alert should nudge him; the email Ali sends
  mentions Pro ($19/10k).

---

## Users snapshot (live, paying in bold)
- **Faisal (faisal@samaed.com, Starter, SA)** — heaviest sender (~3k delivered
  first cycle; 4368 attempted). Healthy on 0.1.21; had 2 transient
  `create_failed` on Jul 2 (campaign create; succeeded on retry — watch if it
  recurs). Post-fix quota: 1750/2500, resets Jul 25.
- **Miriam (miriam@…, US)** — refunded; gift Pro until Jul 24 (see above).
  Sign-in loop RESOLVED (0.1.21): clean login + first-ever compose_view_seen
  on 06-29. Colleague **annelise@osbornecapitalpartners.com** also has an
  account (same person-id merge in PostHog — query by distinct_id, not
  person_id, for these two).
- Morgan (morgan@morganeverhart.com) — tried to pay Starter, card declined
  (insufficient funds), still free. Decision pending: gentle no-reason-stated
  email vs leave to Stripe dunning. Low priority.
- Alan (alan@sandstone.co.uk) — churned 06-28 ("bugs"); never reached compose.
  Decision: NO win-back email (trust risk); watch activation funnel instead.
- **Activation funnel insight (favorited):**
  https://eu.posthog.com/project/152466/insights/43NTvOQ0 — sign-in → compose
  → send. `compose_view_seen` is the RELIABLE activation gate
  (`sidebar_opened` under-reports: a user sent 8 with 0 sidebar events).

## Quora drip (docs/plans/quora-content.md is the tracker)
6 answers live (2 product : 4 value). Next up: **without-showing-recipients**
(product; sharpened draft in tracker-referenced chat, re-verified 07-01) →
**cold-emails-tool** (draft ready; RE-CHECK answers before posting — the batch
question went 1→8 answers between checks). Hard rules: READ-BEFORE-YOU-WRITE
(scrape + read every existing answer; skip if we can't beat them or add a
distinct angle) · ~2:1 value:product · no in-body links (profile bio has
getoutmass.com) · skip spam-adjacent questions (email-bombing, Kali365
security-fear) · profile photo stays the real face, not the logo.

## Conventions / process (unchanged, keep doing)
- **CLAUDE.md #1:** approval before user-affecting changes (API/schema/
  extension/auth/pricing/quota); deploy sanity-check; no push without OK.
  Money/refunds/credentials = Ali only, never the assistant.
- **Commit trailer:** `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`
- **Backend tests:** `cd backend && export JWT_SECRET=$(python -c "import
  secrets; print(secrets.token_hex(32))") && python -m pytest tests/<files> -q
  --no-header -p no:cacheprovider` — run SPECIFIC files (full suite has a
  pre-existing collection quirk). FakeSupabase ignores `.eq` filters.
  conftest FAKE_USER anchor is dynamic (1st of current UTC month) — a
  hardcoded past date would trip the /send-gate rolling reset in tests.
- **Extension:** `node --check` each JS; locale JSONs validated with
  node JSON.parse; version bump per store upload; zip via .NET ZipArchive
  (forward slashes; Windows Compress-Archive is BROKEN); exclude
  README/CHANGELOG.
- **Railway:** 3 services (web/worker/beat) share code but have SEPARATE env
  vars — set secrets on the service that runs the code (bit us with Telegram).
  Web has healthcheck → zero-downtime.
- **PostHog EU project 152466** via `mcp__plugin_posthog_posthog__exec`
  (info before call). Ultracode on → use Workflow for substantive
  investigations/reviews (the 3-lens adversarial review caught 1 blocker +
  6 major pre-commit — worth it every time on billing-critical code).
- Support emails BCC `outmassapp@outlook.com`.

## Backlog beyond the above (see backlog.md for detail)
- **Quota follow-ups (deferred, bounded):** mid-campaign increment race (batch
  increments in 5 send loops) · cancel_at_period_end final-day refill ·
  month-end anchor-day drift (29-31 anchors) · create-checkout StripeError
  dual-subscription fallthrough.
- Edge 0.1.22 upload · support send-as + DKIM (P3) · separate game events from
  PostHog (P3) · PostHog funnel re-run (consent_declined trend after the
  0.1.21 explainer) · send-pacing Phase 2/3 (deferred) · Telegram report v2
  (PostHog section).
