# Session Handoff — 2026-07-15 (pre-compaction)

Continuity doc so work resumes cleanly after compaction. Companions:
`backlog.md` (prioritized TODO) · `handoff-2026-07-03.md` (previous handoff) ·
`2026-07-06-competitive-analysis-securemailmerge.md` (competitor doc) ·
`quora-content.md` (content drip). Internal — `docs/plans/` is excluded from
the public Jekyll site.

---

## Where things stand

### Git / deploy state
- **origin/master = `1b103a3`** — everything pushed. Site (GitHub Pages) and
  backend (Railway) deploy from this repo on push.
- **Backend LIVE on `api.getoutmass.com`** (Cloudflare-proxied, SSL Full;
  verified: `/auth/login` 307 → `redirect_uri=https://api.getoutmass.com/auth/callback`).
  `outmass-production.up.railway.app` stays bound as silent fallback. Railway
  `BACKEND_URL` env flipped ✅; Azure app has BOTH callback URIs registered ✅.
  One Railway deploy failed at "Configure network" (their infra, transient) —
  the retry succeeded.
- **Extension: Chrome = 0.1.25 LIVE.** Edge = 0.1.25 submitted, in review
  (Edge Partner Center now HAS a host-permission-justification field — it was
  stale, updated with the honest 6-host text on 07-15).
- **Migration state:** 021, 022, **023 (`daily_send_cap`)** all run by Ali ✅.

### Release arc 0.1.23 → 0.1.25 (all in one week)
- **0.1.23 (07-08):** Microsoft is migrating work Outlook to
  **outlook.cloud.microsoft** (tenant-by-tenant since Nov 2025) — content
  script never matched there; hit paying customer Mary
  (business@solutionsweekly.info). Added the domain (6 places) + retry-until-ack
  for SHOW_SIDEBAR. First host-permission re-approve prompt; comms went out
  BEFORE rollout (lesson: warning must precede the event).
- **0.1.24 (07-14):** zh-CN incident fixes — CSV decode chain
  utf-8→gb18030→big5 (fixtures-tested), honest network-failure UX
  (`network:true` classification + localized alert + connectivity banner +
  20s AbortController), telemetry fidelity (`create:/upload:/send:` prefixed
  real errors). Silent update (no new permissions).
- **0.1.25 (07-15):** never-signed-in gate (3rd reauth-banner state +
  `auth_required` flag + `alertSignInFirst` ×11), first-run **welcome.html**
  tab, **API on api.getoutmass.com with railway fallback + sticky base**
  (OAuth launch, HEALTH_CHECK, feedback, error-report all use it), and the
  **daily send cap** (below). New host permission → re-approve prompt;
  bulk email + in-app announcement sent BEFORE rollout.

### The July-14 incident (fully resolved, identity found)
Anonymous zh-CN user (distinct_id `dfd594b2…`): GBK CSV rejected 7×, then 6×
`create_failed`, clicked Upgrade twice, "deleted" a nonexistent account, kept
retrying. Root cause was NOT network/GFW (first hypothesis, corrected): user
**never signed in** — zero oauth events ever; every backend call died at the
no-JWT short-circuit; the sidebar showed NO sign-in affordance pre-0.1.25 and
the raw English "Not authenticated. Please login." read as a paywall.
**She is Charlotte Tang <charlotte@aeezo.com>** — support email arrived (in
junk!) 15 min after her last telemetry error. Same company as
marketing.m@aeezo.com (ICP marketing user who onboarded 07-13) → **aeezo is a
team evaluating OutMass.** Recovery email sent with the sign-in path; watch
for her first `oauth_started`.

### Daily send cap (shipped same-day from a customer complaint)
- **Trigger:** marketing@bellmed.com upgraded to Starter because
  blog/outlook-mail-merge-limit.html FALSELY claimed auto daily-cap spreading.
  Ali chose: build it immediately rather than refund. Feature shipped in
  0.1.25; "we bumped it to the top for you" email sent; bellmed = **4th
  paying Starter** (real MRR now 4×$9 = $36).
- **Design:** `campaigns.daily_send_cap` (migration 023). Scheduled worker
  sends ≤cap per beat-day, re-queries resumable contacts, advances
  `scheduled_for` +24h and stays 'scheduled' until exhausted. Create endpoint:
  cap requires `scheduled_for`, clamps 5000, 0→None. Sidebar: field in
  Schedule section (auto-schedules now+2min if cap set with no date);
  **mutually exclusive with A/B** (UI lock + send-flow guard). v1 caveats
  (documented): follow-up timing keyed to first batch; A/B excluded.
- Tests: `backend/tests/test_daily_cap.py` (6).

### Claims audit (23-agent workflow + grep sweep; 14 fixes on 9 pages, `a7b1b3e`)
All confirmed in code, 0 rejected in adversarial verify. Decision (Ali):
**follow-ups stay Pro-only in COPY** (matches code gate campaigns.py:1027) —
"itiraz gelirse düşünürüz". Fixed: Free/Starter follow-up promises removed
(Pro card gains "Automated follow-up (non-openers)"; Starter cards gained the
real "Daily send limit"); "follow-up sequences that stop when a recipient
replies" (reply does NOT cancel follow-ups — verified, worker only filters
opened_at/clicked_at); "We never store your email content" honestly rewritten
(pricing FAQ + mail-merge post); OneDrive "view analytics" fabrication ×5
across 3 posts; AI-writer per-recipient claim; "across timezones"; 1-second→2s;
"Settings → Templates" → Campaign tab in all 3 competitor guides; limit-post
paragraph now describes the SHIPPED cap. **Leftovers in backlog:**
`popupConsentExplainer` ("never store your email content", 11 locales →
soften in 0.1.26) and **reply-cancels-followup** as a real small feature.
**New discipline: site may only claim what's LIVE — push marketing claims
AFTER the store publishes the release.**

### Test procedure (institutionalized, in CI + CLAUDE.md)
`node extension/tests/run-all.js` — mandatory before any zip:
csv-decode (real decodeCsvBuffer vs 10 encoding fixtures) ·
locale-consistency (11 locales key parity + placeholders; first run caught 3
keys missing in ALL 10 non-en locales) · i18n-usage (literal keys exist in
en). CI job `extension-checks` runs it on every push. Zip build excludes
`extension/tests/` (build script in session scratchpad; recreate per handoff
conventions if lost — .NET ZipArchive, forward slashes, exclude
README/CHANGELOG/tests).

### Onboarding emails + page (live)
- **Welcome email** on first-ever sign-in (`upsert_user` returns
  `(user, created)`; both auth paths; MailerSend best-effort; never raises).
- **Upgrade thank-you** on first Stripe checkout (rides the webhook replay
  guard; quota from `monthly_limit_for_plan`; "billing month starts today").
- **welcome.html** opens on install (`onInstalled`, reason=install) — 3 steps,
  work/personal Outlook buttons, CSV-UTF-8 tip.
- Verify deliverability when a new signup lands (Railway log line
  `Welcome email dispatched to …`; spam-vs-inbox unknown → DKIM/DMARC in
  backlog P3).

### Telegram report v2 (deployed)
- 🩺 **Errors (12h)** from PostHog Query API (HARD flags ⚠️: $exception,
  send/csv/test/ai failures; oauth_failed + uninstall = "(info)") +
  🌐 API health line + **MRR counts PAYING only** (live stripe_subscription_id,
  minus REPORT_OWNER_EMAILS) with "Gifts/comp active: N (not counted)".
- Worker env set ✅: POSTHOG_PERSONAL_API_KEY, REPORT_HEALTH_URL,
  REPORT_OWNER_EMAILS. **Web `REPORT_TRIGGER_KEY` still NOT set** → manual
  `POST /api/admin/trigger-report` 404s; cron (05/17 UTC) works regardless.
- The 07-15 morning report's error section WORKED (caught the zh incident).

### Tracking bot-evidence v0 (deployed 07-09, data accruing)
`/t` and `/c` stamp `events.metadata` with ua/ip/secs_since_sent. Greg's 260-
send showed 93% click-to-open = corporate scanner signature. **~07-16+: run
the analysis SQL** (in chat/commit 029b16d message) and design v1 filtering
thresholds. Follow-up correctness depends on it (not_opened contaminated by
scanner opens).

## Users snapshot
- **Paying (4 × Starter = $36 MRR):** Faisal (faisal@samaed.com — batch
  sender, ~3k/cycle, upsell candidate; **first rolling reset + Stripe renewal
  2026-07-25 — field proof of quota epic**) · Greg Sanders
  (gsanders@overdeliverinc.com — cleanest activation, 260-recipient campaign
  07-08, scheduling enabled, watch 2nd campaign) · Mary Bass
  (business@solutionsweekly.info — cloud.microsoft victim, unblocked on
  0.1.23+, panel opens, first campaign still pending) · **bellmed**
  (marketing@bellmed.com — upgraded FOR the daily cap; watch first capped
  campaign; personal "we built it for you" email sent).
- **Gifts (auto-expire via expire_manual_promos beat; all
  stripe_subscription_id NULL):** Surendra starter → **2026-07-22** · Miriam
  pro → **2026-07-24** · Alan pro → ~2026-08-06 (chose SecureMailMerge; per-
  recipient real attachments gap — see competitive doc).
- **aeezo team:** marketing.m@aeezo.com (onboarded, 1 benign oauth_failed) +
  Charlotte (see incident). High-value ICP; watch conversions.
- nina.ma_@outlook.com — new 07-13, clean activation start, first welcome-
  email recipient.
- North-star: users sending in 2+ distinct weeks ≈ 1 (Faisal). PMF gate ~5.

## Support / process rules (unchanged + new)
- Approval before user-affecting changes; no push without explicit OK; money/
  credentials = Ali only. Commit trailer: `Co-Authored-By: Claude Opus 4.8 (1M context) <noreply@anthropic.com>`.
- Support: async email only (NO screenshare/calls — memory support_async_only);
  BCC outmassapp@outlook.com; gift test before discretionary comps (memory
  gift_learning_or_crutch; bug-comp exempt).
- **JUNK RULE (open item!):** two support emails lost to junk (Alan, Charlotte)
  — Cloudflare Email Routing forward breaks SPF alignment. Ali must add
  Outlook rule "sent to support@getoutmass.com → move to Inbox". UNCONFIRMED.
- **Claims discipline:** never publish a capability claim before the feature
  is live in users' hands; extension release checks are mandatory (CLAUDE.md
  Test section); every new user-visible error message must say what to DO.
- Backend tests: run SPECIFIC files (JWT_SECRET env; conftest FAKE_USER
  month_reset_date is dynamic). PostHog EU project 152466 (exec tool; also
  receives a game's events — separation in backlog). Railway env per-service.

## Watch list / dated
- **Now:** Charlotte's first oauth_started · bellmed's first capped campaign
  (campaign row with daily_send_cap in Supabase; status flipping
  scheduled→sending→scheduled daily) · Edge 0.1.25 approval · in-app
  announcement SQL for 0.1.25 (given to Ali; UNCONFIRMED run) · junk rule.
- **~07-16:** bot-evidence analysis (v1 filter design).
- **07-22:** Surendra promo expiry → flips to free (beat).
- **07-24:** Miriam promo expiry → flips to free.
- **07-25:** Faisal first true rolling reset + Stripe renewal.
- Backlog headliners: e2e CI red since 06-03 (chrome stub fix) · quota
  follow-ups (4 bounded) · popupConsentExplainer 0.1.26 · reply-cancels-
  followup · DKIM/DMARC · Office Add-in = #1 post-PMF option · marketing
  ladder (review-ask ON HOLD until happy retained users; demo video script
  ready in chat 07-06; PH launch later).
